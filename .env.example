# Copy to .env or export these from your shell profile.
# Only needed when the configured provider talks to a hosted service.

# Name matches llm.api_key_env in config.toml
HEIDR_LLM_KEY=

# Used only when stt.provider = "api"
HEIDR_STT_KEY=
